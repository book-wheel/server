name: Deploy to EC2

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "Legacy EC2 deployment is disabled."
          echo "Current production deployment is handled manually on the lab server with Docker Compose."
